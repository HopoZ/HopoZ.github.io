---
date: 2025-03-05 01:46:08
title: 200. 岛屿数量
categories: algo
tags:
  - leetcode
toc: true
timeline: code
---

> Problem: [200. 岛屿数量](https://leetcode-cn.com/problems/number-of-islands/)

# 解析

## DFS
当第一次遇见陆地时，深度遍历这个岛屿，把每个地点打上编号，遇见的陆地都会被标记为已访问，这样就可以避免重复访问。

```C++
//! m*n,m*n -> 深度遍历最高耗时m*n;函数递归栈最高占地m*n
class Solution {
private:
  void modifyGrid(vector<vector<char>> &grid, int i, int j) {
    if (i < 0 || i >= grid.size() || j < 0 || j >= grid[0].size() ||
        grid[i][j] != '1') {
      return;
    }
    grid[i][j] = '2';
    vector<vector<int>> dir = {{1, 0}, {-1, 0}, {0, 1}, {0, -1}};
    for (auto e : dir) {
      int newI = i + e[0];
      int newJ = j + e[1];

      modifyGrid(grid, newI, newJ);
    }
  }

public:
  int numIslands(vector<vector<char>> &grid) {
    int res = 0;
    for (int i = 0; i < grid.size(); ++i) {
      for (int j = 0; j < grid[0].size(); ++j) {
        if (grid[i][j] == '1') {
          ++res;
          modifyGrid(grid, i, j);
        }
      }
    }
    return res;
  }
};
```

## BFS
广度优先遍历，遇到陆地就把这个岛屿陆地都标记为已访问。

```C++
//! m*n,1 -> 广度遍历最高耗时m*n;占地常数
